set(PACKAGE_VERSION @INSTRUMENTATION_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@INSTRUMENTATION_VERSION_MAJOR@")
  if ("${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@INSTRUMENTATION_VERSION_MINOR@")
    set(PACKAGE_VERSION_EXACT TRUE)
  elseif("${PACKAGE_FIND_VERSION_MINOR}" LESS "@INSTRUMENTATION_VERSION_MINOR@")
    set(PACKAGE_VERSION_COMPATIBLE TRUE)
  else()
    set(PACKAGE_VERSION_UNSUITABLE TRUE)
  endif()
else()
  set(PACKAGE_VERSION_UNSUITABLE TRUE)
endif()
